# تغییرات پروژه (CHANGELOG) - Zarfolio

تمامی تغییرات مهم و قابلیت‌های اضافه شده به سیستم Zarfolio در این فایل ثبت می‌شوند.

---

## [0.1.4 BETA] - 2026-09-17

### ماژول ری‌گیری (Refining Module)
* **دریافت شرطی طلا با عیار موقت ۷۵۰**:
  * حذف الزام ورود دستی عیار رسمی توسط کاربر در فرم ثبت دریافت شرطی؛ طلا تا مشخص شدن نتیجه آزمایشگاه ری‌گیری با عیار موقت ۷۵۰ و نوع موجودی شرطی (`conditional`) در انبار طلا ثبت می‌شود.
  * نمایش بج‌های تفکیکی «شرطی (موقت)» و «آبشده قطعی» در جدول اقلام خروجی پرونده ری‌گیری.
* **ثبت عیار اعلامی آزمایشگاه و تبدیل خودکار به آبشده**:
  * افزودن فیلد «عیار اعلامی آزمایشگاه (نتیجه ری‌گیری)» در فرم دریافت پاکت در صفحه پاکت‌های ریگیری و مودال جزئیات پرونده.
  * افزودن امکان «ثبت عیار» مستقیم برای پاکت‌های نمونه ری‌گیری (`POST /api/refining/packets/[id]/purity`).
  * با ثبت عیار نتیجه آزمایشگاه، تمام اقلام طلای شرطی پرونده متناظر و رکوردهای موجودی انبار طلا (`metal_inventory`) به صورت خودکار به «آبشده قطعی» (`melted`) تبدیل شده و وزن معادل ۷۵۰ بر اساس عیار واقعی آزمایشگاه محاسبه و به‌روزرسانی می‌شود.
* **ویرایش تب «خروج طلا» و انتخاب نوع طلا (آبشده، متفرقه، شرطی، سواله)**:
  * الهام‌گیری از تب خروج فلزات خام در بخش ثبت سند و پیاده‌سازی سلکتور چهارگانه نوع طلا (آبشده، متفرقه، شرطی، سواله) در فرم «ثبت خروج طلا به ریگیر».
  * نمایش شرطی و هدفمند فیلدهای شماره انگ و نام آزمایشگاه بر اساس نوع طلای انتخابی (آبشده یا شرطی).
  * ثبت نوع انتخاب‌شده در رکوردهای انبار (`metal_inventory`) و اقلام ارسالی (`refining_items`) و نمایش ستون «نوع طلا» در جدول اقلام تحویلی به ریگیر.
* **به‌روزرسانی شمایل تب‌های پرونده ری‌گیری به سبک Appica UI Tabs**:
  * پیاده‌سازی الگوی تب‌های مدرن Pill Track مطابق استانداردهای طراحی `appica.dev` شامل ترَک پس‌زمینه با گوشه‌های گرد، تب‌های فعال برجسته و لایه‌ای با Shadow، آیکون‌های متناسب و بج‌های شمارنده تعبیه‌شده در کنار عناوین تب‌ها.
* **تست‌ها و صحت عملکرد**:
  * افزودن تست‌های جامع برای ثبت طلای شرطی، تبدیل خودکار به آبشده با دریافت عیار پاکت و متد اختصاصی تسویه عیار در مجموعه تست‌های `refining-module.test.ts`.

---

## [0.1.3 BETA] - 2026-09-16

### ثبت سند
* افزودن بخش «پیش‌نمایش مانده طرف‌حساب» شامل «مانده قبلی»، «اثر این سند» و «مانده پس از ثبت سند» در بخش ردیف‌های سند.
* اتصال مستقیم پیش‌نمایش مانده به موتور مرکزی حسابداری از طریق API اختصاصی `/api/transactions/preview-balance`.
* به‌روزرسانی لحظه‌ای پیش‌نمایش مانده با تغییر طرف‌حساب، مبالغ، اوزان و ردیف‌های سند به همراه کنترل درخواست با Debounce و AbortController.
* جلوگیری از محاسبات مستقل و موازی مانده در Frontend.

### مدیریت موجودی
* پشتیبانی از خروج مرحله‌ای موجودی طلا و فلزات برای آبشده، متفرقه، شرطی و سواله.
* محاسبه و نمایش تفکیکی «موجودی اولیه»، «خروج موقت در سند جاری» و «باقیمانده قابل انتخاب» در لیست انتخاب موجودی.
* اعمال محدودیت حداکثر وزن قابل انتخاب در Frontend و کنترل سخت‌گیرانه سمت Backend در هنگام ثبت سند.
* قفل‌سازی عیار موجودی انتخابی بر روی Frontend (Readonly) و اعتبارسنجی دقیق عیار در Backend با رکورد مرجع موجودی.
* آزادسازی کامل موجودی رزروشده هنگام لغو، ویرایش یا حذف ردیف‌های سند قبل از ثبت نهایی.
* پیشگیری از شرایط رقابتی (Race Condition) و خروج بیش از موجودی واقعی در دیتابیس.

### اعتبارسنجی و امنیت
* اعمال محدودیت عددی سخت‌گیرانه برای فیلد «شماره پاکت / انگ» در Frontend (حذف کاراکترهای غیرعددی) و اعتبارسنجی الگوی عددی `^[0-9]+$` در Backend.
* اعتبارسنجی مجدد عیار و اوزان در لایه Backend بدون اعتماد به داده‌های Client.

### رابط کاربری (UI/UX)
* اصلاح رنگ، پس‌زمینه، کادر، فوکوس و وضعیت غیرفعال فیلد «نام آزمایشگاه ری‌گیری» و هماهنگ‌سازی کامل آن با سایر ورودی‌های فرم در دو تم روشن و تاریک.
* نمایش Toast اطلاع‌رسانی با متن دقیق «سند با موفقیت نهایی شد.» پس از ثبت قطعی سند.

### دقت و کیفیت
* حفظ دقت اعشاری کامل در تمامی محاسبات وزنی و مالی با پشتیبانی از سیستم اعشار تنظیمات Zarfolio.
* اجرای تست‌های واحد برای پیش‌نمایش مانده، خروج مرحله‌ای موجودی و اعتبارسنجی شماره پاکت/انگ.
