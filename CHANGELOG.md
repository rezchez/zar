# تغییرات پروژه (CHANGELOG) - Zarfolio

تمامی تغییرات مهم و قابلیت‌های اضافه شده به سیستم Zarfolio در این فایل ثبت می‌شوند.

---

## [0.1.6 BETA] - 2026-09-20

### هماهنگی سایدبار با ردیف‌های پین‌شده سند (Sidebar & Pinned Document Lines Docking)
* **اتصال هوشمند فوتر چسبان سایدبار به لبه بالایی ردیف‌های سند**:
  * حذف فاصله کناری و امتداد کامل پنل ردیف‌های پین‌شده به صورت تمام‌عرض در انتهای صفحه (`fixed bottom-0 left-0 right-0 z-40`).
  * اندازه‌گیری پویا و بدون وقفه ارتفاع پنل پین‌شده با استفاده از `ResizeObserver` و تنظیم متغیر سراسری `--document-lines-pinned-height`.
  * تنظیم پویای ارتفاع سایدبار اصلی بر اساس `calc(100vh - var(--document-lines-pinned-height))` به نحوی که فوتر چسبان سایدبار دقیقاً مماس و متصل به مرز بالایی (`border-t-2 border-amber-500`) پنل ردیف‌های پین‌شده قرار گیرد.
  * بازگشت خودکار ارتفاع سایدبار و رفع کلاس‌ها بلافاصله پس از لغو حالت پین یا بستن صفحه.

### پایگاه داده طرف‌حساب‌های ستاره‌دار (Dedicated Favorite Customers Collection)
* **انتقال داده‌ها به کالکشن اختصاصی در دیتابیس PocketBase**:
  * ساخت کالکشن مستقل و ایمن `favorite_customers` با رابطه‌های مستقیم `user` و `customer`، قانون حذف آبشاری (`cascadeDelete`) و ایندکس یکتای ترکیبی `idx_fav_user_customer`.
  * توسعه اندپوینت محافظت‌شده `/api/customers/favorites` با متدهای `GET` و `POST` جهت دریافت و تاگل ستاره‌دار بودن با اعمال محدودیت نرخ درخواست (`rateLimit`) و احراز هویت سرور.
  * بازنویسی هوک کلاینتی `useFavoriteCustomers` با همگام‌سازی ابری در دیتابیس، کش سریع محلی در `localStorage`، به‌روزرسانی آنی رابط کاربری (Optimistic UI) و بازیابی خودکار در صورت خطا.

### بهبودهای بصری و کارایی در ثبت سند (Document Form UX & Performance)
* **طراحی کم‌حجم و مربعی بخش نمایش وضعیت حساب و طلب/بدهی**:
  * بازطراحی المان‌های نمایش وضعیت طلب و بدهی طرف‌حساب انتخاب‌شده به ساختار مربعی و فشرده جهت کاهش فضای اشغال‌شده و دسترسی سریع به ترازهای ریالی و وزنی.
* **بارگذاری تدریجی ۱۰ نفره با اسکرول نامحدود در لیست طرف‌حساب‌ها**:
  * محدودسازی نمایش اولیه به ۱۰ نفر اول در انتخابگر طرف‌حساب و بارگذاری خودکار و پیوسته مخاطبان بعدی با اسکرول کردن کاربر جهت کاهش بار محاسباتی DOM و افزایش سرعت رندر.
* **دکمه ثبت ردیف در حالت پین‌شده و عنوان پویا بر اساس تب و نوع عملیات**:
  * تعبیه دکمه «ثبت ردیف» در نوار عملیات ردیف‌های سند در حالت پین‌شده قبل از دکمه «ثبت موقت».
  * تغییر خودکار و لحظه‌ای عنوان دکمه متناسب با تب جاری و نوع عملیات (مانند «ثبت ردیف ورود آبشده»، «ثبت ردیف خروج آبشده»، «ثبت ردیف ورود متفرقه»، «ثبت ردیف خرید ارز»، «ثبت ردیف دریافت نقد» و...) به همراه پیشوند «ویرایش ردیف» در حالت ویرایش.
* **بهینه‌سازی کادرهای پیش‌نمایش مانده و طراحی افقی کم‌ارتفاع**:
  * کاهش چشمگیر ارتفاع کادرهای مانده قبلی، اثر این سند و مانده نهایی از طریق چیدمان افقی و یک‌خطی.
  * افزایش عرض متناسب، حذف نوار اضافه خالص اثر سند، و ساده‌سازی وضعیت دوخطی اثر سند به برچسب تک‌خطی «بدون اثر».
* **اصلاح نمایش ارز پایه و رفع تکرار عنوان واحد پول در ثبت سند**:
  * رفع تکرار نام واحد پول در بالای فرم ثبت سند (نمایش شکیل ریال ایران (IRR) و تومان (IRT)) و پاکسازی برچسب پیش‌نمایش مانده طرف‌حساب و نام مشتری از بخش ردیف‌های سند.

### تنظیمات برنامه و نوتیفیکیشن‌ها (Program Settings & Notifications)
* **تب اختصاصی «تنظیمات شماره‌گذاری اسناد» در تنظیمات برنامه**:
  * ایجاد تب نهم با شناسه `document_numbering` در صفحه تنظیمات برنامه و انتقال تنظیمات شروع شماره فاکتور و سند به این تب جهت تفکیک دقیق‌تر تنظیمات اسناد حسابداری.
* **اعلان‌های توست (Toast) در ذخیره‌سازی تنظیمات و حذف بنرهای ثابت**:
  * جایگزینی پیام‌ها و بنرهای ثابت بالای صفحه در بخش تنظیمات با اعلان‌های توست مدرن، سبک و تعاملی جهت بازخورد سریع نتیجه ذخیره‌سازی.

---

## [0.1.5 BETA] - 2026-09-20

### ارتقای سیستم خروج فلزات و مدیریت موجودی (Metal Outflow & Inventory Management)
* **جدول پیشرفته، تمام‌صفحه و پرسرعت انتخاب موجودی فلزات (`MetalInventoryPicker`)**:
  * جایگزینی منوی کشویی ساده با جدول مودال پیشرفته و لود آنی موجودی بر پایه کش سمت کلاینت.
  * تفکیک دقیق دسته‌بندی‌ها با تب‌های اختصاصی: **آبشده (مفتول/شمش)**، **متفرقه**، **شرطی** و **سواله** به همراه شمارنده اقلام موجود.
  * نمایش تمام‌صفحه پرتال (`createPortal`) در هر دو تب «خروج فلزات» و «فروش فلزات» بدون تداخل با ترنسفورم‌های CSS و کانتینرهای انیمیشن.
  * قفل کامل و قطعی اسکرول صفحه پس‌زمینه (`document.body` و `documentElement` با `overflow: hidden`) و مهار رویدادهای اسکرول موس (`onWheel`).
  * جستجوی سریع و زنده بر مبنای شماره انگ، نام آزمایشگاه ری‌گیری و نام طرف‌حساب.
  * ستون‌های تفکیکی استاندارد: ردیف، مشخصات ری‌گیری و انگ، طرف‌حساب دریافتی، وزن ترازویی (باقیمانده و اولیه) و وزن تبدیل‌شده به عیار معیار هر فلز بر اساس تنظیمات.
* **محاسبه و مارک کردن چندگانه اقلام (Batch Marking & Sum Calculation)**:
  * افزودن ستون چک‌باکس با قابلیت انتخاب سریع همه یا انتخاب ردیف‌های دلخواه.
  * نوار برجسته تجمیعی در پایین جدول با محاسبه لحظه‌ای مجموع وزن ترازویی و وزن عیار معیار اقلام مارک‌شده جهت سهولت در تصمیم‌گیری و مدیریت خروج چند قطعه.
* **پاورقی آماری و تجمیعی موجودی**:
  * نمایش تعداد آبشده‌های دارای انگ، مجموع وزن ترازویی کل موجودی بخش فعال و مجموع وزن تبدیل‌شده به عیار معیار.
* **خلاصه گردش و تاریخچه ریز تراکنش‌های قطعه (`MetalInventoryHistoryModal`)**:
  * تعبیه آیکون تاریخچه در هر سطر جدول و در کارت قطعه انتخاب‌شده.
  * کارت‌های شاخص کلیدی شامل وزن اولیه دریافت، مجموع پرداخت‌ها و مانده قابل استفاده فعلی.
  * تایم‌لاین گام‌به‌گام شامل تاریخ و شخص تحویل‌دهنده اولیه، مراحل مختلف پرداخت/خروج جزئی به اشخاص مختلف همراه با تاریخ، وزن و توضیحات.

### اصلاح رابط کاربری فروش فلزات (Gold Sale UI Fixes)
* **رفع قطعی اشکال افزایش ارتفاع فیلد «نوع فی»**:
  * تثبیت ارتفاع المان `select` روی ۴۰ پیکسل (`h-10 min-h-[40px] max-h-[40px]`) در هر دو حالت محاسبه وزنی و پولی.
  * تراز کردن اقلام گرید فرم (`align-self: start` و `align-items: start`) جهت جلوگیری از کشیده شدن فیلد نوع فی هنگام پدیدار شدن متن حروف قیمت و مبلغ کل.

### همگام‌سازی تب‌های ثبت سند با هش URL و سیستم قفل طرف‌حساب (Document Tabs URL Sync & Customer Lock)
* **لینک‌دهی و هش‌گذاری تب‌های ثبت سند (`DocumentEntryTabs`)**:
  * تبدیل دکمه‌های تب‌ها به پیوند دارای هش استاندارد (`#metals`، `#gold-sale`، `#goods`، `#currency`، `#stone`، `#coin`، `#cash`، `#bank`، `#income-expense`، `#claim`، `#workmanship`).
  * همگام‌سازی آنی تب فعال با هش نشانی صفحه (`window.history.replaceState`) و پشتیبانی از پارامتر `tab` در جستجوی URL.
  * جلوگیری از پرش به تب اول هنگام رفرش صفحه و باز شدن مستقیم فرم روی همان تب انتخابی.
  * امکان اشتراک‌گذاری، بوک‌مارک و کپی لینک مستقیم به هر یک از تب‌های سند.
* **دکمه آیکونی قفل طرف‌حساب با راهنمای اختصاصی (`Customer Lock & Info Tooltip`)**:
  * استقرار دکمه قفل مستقیماً جلوی فیلد انتخاب طرف‌حساب (در سطر اینپوت جستجو و در کارت طرف‌حساب انتخابی) با حذف متن و چک‌باکس اضافی جهت زیبایی و ارگونومی رابط کاربری.
  * نمایش وضعیت قفل با آیکون‌های پویا:
    * در حالت فعال: قفل بسته (`Lock`) با برجستگی کهربایی و رینگ طلایی.
    * در حالت غیرفعال: قفل باز (`Unlock`) با رنگ خاکستری خنثی.
  * تعبیه آیکون راهنما `(i)` در کنار دکمه قفل با پنجره شناور (Tooltip) جهت توضیح عملکرد قابلیت برای کاربران.
  * ذخیره‌سازی نام و شناسه طرف‌حساب در حافظه مرورگر کاربر (`localStorage`).
  * بازیابی خودکار طرف‌حساب قفل‌شده بلافاصله پس از رفرش صفحه ثبت سند، بدون نیاز به انتخاب مجدد.
  * لغو و پاکسازی هوشمند قفل در صورت کلیک مجدد روی قفل یا زدن دکمه «تغییر طرف‌حساب».

---

## [0.1.4 BETA] - 2026-09-19

### طرف‌حساب‌های ستاره‌دار و مدیریت ترجیحات کاربران (User Preferences & Starred Customers)
* **سیستم انتخاب و نشانه‌گذاری طرف‌حساب‌های پرکاربرد (Favorite / Starred Customers)**:
  * افزودن قابلیت ستاره‌دار کردن یک یا چند طرف‌حساب جهت دسترسی سریع به مخاطبان پرتکرار.
  * تفکیک کامل و ایزولاسیون طرف‌حساب‌های ستاره‌دار برای هر کاربر به صورت مستقل در پایگاه داده.
  * ایجاد کالکشن اختصاصی `user_preferences` در PocketBase با قوانین دسترسی امن بر مبنای `user = @request.auth.id`.
  * پیاده‌سازی هوک مشترک `useFavoriteCustomers` با همگام‌سازی ابری و کش سریع محلی در مرورگر.
  * اولویت‌بندی خودکار طرف‌حساب‌های ستاره‌دار در صدر لیست پیشنهادات هنگام جستجو یا انتخاب طرف‌حساب.
  * دکمه فیلتر «ستاره‌دارها (تعداد)» در نوار بالای انتخابگر طرف‌حساب جهت پالایش آنی لیست.
  * دکمه نشانه‌گذاری ستاره در سطر هر مخاطب و در کارت طرف‌حساب انتخاب‌شده با بازخورد بصری طلایی.
  * فعال‌سازی همزمان در فرم ثبت سند (`DocumentForm`) و ماژول ری‌گیری (`RefiningCustomerPicker`).

### رند کردن محاسباتی و سرفصل‌های تعدیلات (Amount Rounding & Accounting Adjustments)
* **مودال سراسری رند کردن مبالغ با قفل اسکرول پس‌زمینه (Amount Rounding Modal)**:
  * پیاده‌سازی مودال تنظیمات رند کردن مبلغ کل به صورت تمام‌صفحه و متصل به ریشه سند (`createPortal`).
  * قفل کامل و قطعی اسکرول صفحه زیرین مودال (`document.body` و `documentElement` با `overflow: hidden`) و جلوگیری از انتشار رویداد اسکرول موس (`onWheel`).
  * امکان رند کردن مبلغ به ۲ رقم (صدگان)، ۳ رقم (هزارگان) یا ۴ رقم (ده‌هزارگان) به روش‌های ریاضی (`Math.round`)، به بالا (`Math.ceil`) یا به پایین (`Math.floor`) و حذف رندهای ۵ و ۶ رقمی طبق درخواست کاربری.
  * ذخیره ابری ترجیحات رندسازی هر کاربر در کالکشن `user_preferences` و بازیابی خودکار در مراجعات بعدی.
* **تراز سرفصل‌های حسابداری تعدیلات ناشی از رند کردن (Rounding Adjustment Journal Lines)**:
  * ثبت خودکار خط ژورنال مالی برای مابه‌التفاوت رندسازی جهت برقراری تراز دقیق اسناد:
    * بستانکار کردن سرفصل ۴۳۰۰ (سایر درآمدهای عملیاتی / تعدیلات ناشی از رند کردن) در زمان رند شدن به بالا.
    * بدهکار کردن سرفصل ۶۵۰۰ (سایر هزینه‌های عملیاتی / کسری ناشی از رند کردن) در زمان رند شدن به پایین.
* **نمایش معکوس واحد پولی به حروف در فیلدهای محاسباتی (Inverse Currency Text Words)**:
  * نگارش حروفی مبالغ با واحد معکوس برای سهولت خوانش: نمایش به تومان در زمان ریال بودن واحد اصلی برنامه و نمایش به ریال در زمان تومان بودن آن.
* **بهینه‌سازی رابط کاربری فروش فلزات**:
  * کوچک‌تر و متناسب‌تر کردن دکمه‌های انتخاب نوع فروش (فروش آب‌شده، فروش متفرقه، فروش بدون تسویه).
  * قفل و غیرقابل‌ویرایش شدن خودکار فیلد «شماره پاکت / انگ» هنگام انتخاب قطعه از موجودی صندوق طلای آب‌شده.

### زیرساخت مرکزی مدیریت و بهینه‌سازی تصاویر (Image Service)
* **معماری و ارکستراتور مرکزی تصاویر بر پایه Sharp و فرمت مدرن WebP**:
  * پیاده‌سازی ماژول مرکزی و استاندارد `ImageService` با رویکرد توسعه‌پذیر برای انواع فایل‌های تصویری سیستم (پروفایل، طرف‌حساب، کالا، جواهر، اسناد).
  * فشرده‌سازی و تبدیل قطعی تمامی تصاویر به فرمت مدرن `WebP` با ضریب کیفیت ۸۵ به همراه حفظ کانال آلفا (شفافیت/Transparency).
  * کراپ هوشمند به مربع ۲۵۶×۲۵۶ پیکسل بدون افت نسبت ابعاد و بدون کشیدگی (Smart Entropy Cropping).
  * اعتبارسنجی چندلایه سمت سرور بر اساس بایت‌های جادویی (Magic Bytes) جهت مقابله با جعل پسوند (MIME spoofing).
  * حذف قطعی تمامی متادیتاهای حریم خصوصی EXIF، موقعیت جغرافیایی GPS و مشخصات دوربین عکاسی.
  * ساختار نام‌گذاری امن با هگز تصادفی ضد حملات Path Traversal (`avatar_<entityId>_<timestamp>_<randomHex>.webp`).
  * مکانیزم ابطال کش مرورگرها با افزودن خودکار پارامتر نسخه زمانی (`?v=...`) بر اساس زمان به‌روزرسانی رکورد.
  * فعال‌سازی برای عکس پروفایل کاربران (User Avatar) در تنظیمات حساب کاربری و لیست کاربران در مدیریت سیستم.
  * اجرای اسکریپت مایگریشن خودکار و تکرارپذیر (Idempotent) جهت تبدیل آواتارهای قبلی به WebP با کاهش حجم تا ۹۶٪ و پاکسازی امن فایل‌های قدیمی.

### ماژول ری‌گیری طلا (Refining Module)
* **اصلاح عنوان وضعیت پرونده ری‌گیری به «در انتظار تعیین عیار»**:
  * به‌روزرسانی مقدار وضعیت `partially_received` از عنوان قدیمی «دریافت بخشی از طلا» به عنوان دقیق «در انتظار تعیین عیار» در تمام فایل‌های تایپ، تب‌های فیلتر پرونده و جدول داده‌ها.
* **بهینه‌سازی جدول مدیریت پرونده‌ها و حذف کامل اسکرول افقی**:
  * افزودن ویژگی‌های `noHorizontalScroll` و `dense` به کامپوننت `AppicaDataTable` جهت نمایش فشرده و خوانا.
  * خلاصه‌سازی عناوین ستون‌ها («ریگیر»، «تاریخ»، «ارسال»، «دریافت»، «مانده»، «اجرت») و تنظیم ابعاد سلول‌ها جهت جاگیری کامل جدول در یک صفحه بدون نیاز به اسکرول عرضی.
* **انتخابگر پیشرفته و سرچ‌پذیر طرف‌حساب (RefiningCustomerPicker)**:
  * پیاده‌سازی کامپوننت مدرن انتخابگر طرف‌حساب الهام‌گرفته از فرم ثبت سند.
  * جستجوی زنده در نام، کد مشتری، شماره تماس و گروه طرف‌حساب به همراه پشتیبانی کامل از کلیدهای کیبورد (ArrowDown، ArrowUp، Enter، Escape).
  * نمایش نشان آواتار رنگی با حرف الفبا و برچسب گروه کاری (ریگیر، همکار و...).
  * کارت نمایش طرف‌حساب انتخاب‌شده با دکمه تغییر طرف‌حساب و هماهنگی کامل با پوسته تاریک و روشن.
  * جایگزینی سلکتور ساده قبلی در مودال ایجاد پرونده ری‌گیری با کامپوننت پیشرفته جدید.

### رفع خطاها و بهبودهای حسابداری
* **رفع خطای ثبت اجرت ری‌گیری (Failed to create record)**:
  * اصلاح مقدار وضعیت تراکنش ارسالی به کالکشن `transactions` از `'final'` به `'posted'` مطابق با اسکیمای استاندارد پایگاه‌داده PocketBase.
  * ارسال شرطی فیلد `customerCode` تنها برای مقادیر بزرگتر یا مساوی ۱ جهت جلوگیری از نقض شرط `min: 1` پاکت‌بیس در زمان مقدار صفر یا نامشخص.
  * استخراج و نمایش خطاهای اعتبارسنجی فیلدی پاکت‌بیس در روت API ثبت اجرت جهت جلوگیری از نمایش پیام‌های خطای خام و نامفهوم.
* **ایمن‌سازی تراکنش‌های انتقال وجه بین‌بانکی**:
  * اصلاح وضعیت تراکنش از `'final'` به `'posted'` و شرطی‌سازی کد مشتری در روت `/api/banks/transfer`.
* **رفع باگ تایپ نشدن در فیلد جستجوی جدول داده‌ها**:
  * سازگاری با ساختار TanStack Table v9 و استیت محلی هماهنگ در `data-table.tsx` جهت واکنش آنی به تایپ کاربر.
* **اصلاح استایل فوکوس فیلترها در تم تیره**:
  * رفع مشکل سفید شدن پس‌زمینه دراپ‌داون در دارک‌مود با افزودن کلاس‌های `dark:focus:bg-slate-800`.

---

## [0.1.3 BETA] - 2026-09-18

### سیستم اعلان و توست مدرن (Appica UI React Toast)
* **پیاده‌سازی کامپوننت Toast اختصاصی برگرفته از استانداردهای Appica UI**:
  * پیاده‌سازی پکیج جامع اعلانات شامل `ToastProvider`, `Toaster`, `useToastManager`، نوار پیشرفت راست‌به‌چپ (RTL Progress Bar)، انیمیشن‌های ورود و خروج با Framer Motion و پشتیبانی از متدهای کاربردی `success`, `error`, `warning`, `info`, `promise`.
  * جایگزینی تمام بنرهای خطا و هشدارهای درون‌صفحه‌ای در ماژول «مدیریت ری‌گیری طلا» (`RefiningManagementClient`, `RefiningTab`, `RefiningPacketsClient`).
  * اتصال کامل سیستم توست به ماژول «ثبت سند» (`DocumentForm` و `DocumentSubmitActions`) جهت نمایش وضعیت ثبت قطعی/موقت، هشدارهای اعتبارسنجی اوزان و عیار، و انتخاب طرف‌حساب.

### ماژول ری‌گیری (Refining Module)
* **ادغام تب «پاکت‌های نمونه» در تب «دریافت شرطی و پاکت ری‌گیری»**:
  * یکپارچه‌سازی فرآیند دریافت طلای شرطی با ثبت شماره پاکت اعلامی از سوی ریگیر (`packetNumber`) و وزن نمونه به صورت یکجا در همان فرم دریافت شرطی.
  * ایجاد خودکار رکورد پاکت نمونه در جدول `refining_samples` همگام با ثبت دریافت طلای خروجی.
  * نمایش یکپارچه جداول اقلام دریافتی، نمونه‌های در انتظار عیارسنجی و تسویه عیار در یک تب واحد و روان.

### سیستم اعلان و توست (Toast System)
* **تنظیم موقعیت نمایش اعلان‌ها به سمت راست (`bottom-right`)**:
  * انتقال و تثبیت کلیه اعلان‌ها، هشدارها و پیام‌های سیستم به گوشه پایین-راست رابط کاربری مطابق استاندارد RTL.

### بهبود اعتبارسنجی و رفع خطاها
* **استخراج و شفاف‌سازی پیام‌های خطای دیتابیس**:
  * تبدیل پیام‌های خام دیتابیس نظیر `Failed to create record.` به متن‌های فارسی دقیق و شفاف با ذکر فیلدها و دلایل نامعتبر بودن.
* **رفع خطای `value.replace is not a function`**:
  * ایمن‌سازی توابع تبدیل ارقام فارسی و پارس تاریخ در فایل‌های `jalali.ts` و فرم ثبت سند جهت پذیرش ایمن مقادیر عددی یا تهی.
* **نرمال‌سازی ارقام در شماره پاکت و انگ**:
  * تبدیل خودکار ارقام فارسی/عربی به انگلیسی در فیلدهای `stampNumber` و `packetNumber` پیش از اعتبارسنجی عبارات باقاعده.
* **رفع تکرار کلیدها در لیست ارزها**:
  * حذف کدهای تکراری ارزها (نظیر `AED`) در دراپ‌داون‌های انتخاب ارز جهت جلوگیری از خطای کلید تکراری در React.

---

## [0.1.5 BETA] - 2026-09-18

### سیستم اعلان و توست مدرن (Appica UI React Toast)
* **پیاده‌سازی کامپوننت Toast اختصاصی برگرفته از استانداردهای Appica UI**:
  * پیاده‌سازی پکیج جامع اعلانات شامل `ToastProvider`, `Toaster`, `useToastManager`, نوار پیشرفت راست‌به‌چپ (RTL Progress Bar)، انیمیشن‌های ورود و خروج با Framer Motion و پشتیبانی از متدهای کاربردی `success`, `error`, `warning`, `info`, `promise`.
  * جایگزینی تمام بنرهای خطا و هشدارهای درون‌صفحه‌ای در ماژول «مدیریت ری‌گیری طلا» (`RefiningManagementClient`, `RefiningTab`, `RefiningPacketsClient`).
  * اتصال کامل سیستم توست به ماژول «ثبت سند» (`DocumentForm` و `DocumentSubmitActions`) جهت نمایش وضعیت ثبت قطعی/موقت، هشدارهای اعتبارسنجی اوزان و عیار، و انتخاب طرف‌حساب.

### ماژول ری‌گیری (Refining Module)
* **ادغام تب «پاکت‌های نمونه» در تب «دریافت شرطی و پاکت ری‌گیری»**:
  * یکپارچه‌سازی فرآیند دریافت طلای شرطی با ثبت شماره پاکت اعلامی از سوی ریگیر (`packetNumber`) و وزن نمونه به صورت یکجا در همان فرم دریافت شرطی.
  * ایجاد خودکار رکورد پاکت نمونه در جدول `refining_samples` همگام با ثبت دریافت طلای خروجی.
  * نمایش یکپارچه جداول اقلام دریافتی، نمونه‌های در انتظار عیارسنجی و تسویه عیار در یک تب واحد و روان.

---

## [0.1.4 BETA] - 2026-09-17

### ماژول ری‌گیری (Refining Module)
* **دریافت شرطی طلا با عیار موقت ۷۵۰**:
  * حذف الزام ورود دستی عیار رسمی توسط کاربر در فرم ثبت دریافت شرطی؛ طلا تا مشخص شدن نتیجه آزمایشگاه ری‌گیری با عیار موقت ۷۵۰ و نوع موجودی شرطی (`conditional`) در انبار طلا ثبت می‌شود.
  * نمایش بج‌های تفکیکی «شرطی (موقت)» و «آبشده قطعی» در جدول اقلام خروجی پرونده ری‌گیری.
* **ثبت عیار اعلامی آزمایشگاه و تبدیل خودکار به آبشده**:
  * افزودن فیلد «عیار اعلامی آزمایشگاه (نتیجه ری‌گیری)» در فرم دریافت پاکت در صفحه پاکت‌های ریگیری و مودال جزئیات پرونده.
  * افزودن امکان «ثبت عیار» مستقیم برای پاکت‌های نمونه ری‌گیری (`POST /api/refining/packets/[id]/purity`).
  * با ثبت عیار نتیجه آزمایشگاه، تمام اقلام طلای شرطی پرونده متناظر و رکوردهای موجودی انبار طلا (`metal_inventory`) به صورت خودکار به «آبشده قطعی» (`melted`) تبدیل شده و وزن معادل ۷۵۰ بر اساس عیار واقعی آزمایشگاه محاسبه و به‌روزرسانی می‌شود.
* **ویرایش تب «خروج طلا» و انتخاب نوع طلا (آبشده، متفرقه، شرطی، سواله)**:
  * الهام‌گیری از تب خروج فلزات خام در بخش ثبت سند و پیاده‌سازی سلکتور چهارگانه نوع طلا (آبشده، متفرقه، شرطی، سواله) در فرم «ثبت خروج طلا به ریگیر».
  * نمایش شرطی و هدفمند فیلدهای شماره انگ و نام آزمایشگاه بر اساس نوع طلای انتخابی (آبشده یا شرطی).
  * ثبت نوع انتخاب‌شده در رکوردهای انبار (`metal_inventory`) و اقلام ارسالی (`refining_items`) و نمایش ستون «نوع طلا» در جدول اقلام تحویلی به ریگیر.
* **به‌روزرسانی شمایل تب‌های پرونده ری‌گیری به سبک Appica UI Tabs**:
  * پیاده‌سازی الگوی تب‌های مدرن Pill Track مطابق استانداردهای طراحی `appica.dev` شامل ترَک پس‌زمینه با گوشه‌های گرد، تب‌های فعال برجسته و لایه‌ای با Shadow، آیکون‌های متناسب و بج‌های شمارنده تعبیه‌شده در کنار عناوین تب‌ها.
* **تست‌ها و صحت عملکرد**:
  * افزودن تست‌های جامع برای ثبت طلای شرطی، تبدیل خودکار به آبشده با دریافت عیار پاکت و متد اختصاصی تسویه عیار در مجموعه تست‌های `refining-module.test.ts`.

---

## [0.1.3 BETA] - 2026-09-16

### ثبت سند
* افزودن بخش «پیش‌نمایش مانده طرف‌حساب» شامل «مانده قبلی»، «اثر این سند» و «مانده پس از ثبت سند» در بخش ردیف‌های سند.
* اتصال مستقیم پیش‌نمایش مانده به موتور مرکزی حسابداری از طریق API اختصاصی `/api/transactions/preview-balance`.
* به‌روزرسانی لحظه‌ای پیش‌نمایش مانده با تغییر طرف‌حساب، مبالغ، اوزان و ردیف‌های سند به همراه کنترل درخواست با Debounce و AbortController.
* جلوگیری از محاسبات مستقل و موازی مانده در Frontend.

### مدیریت موجودی
* پشتیبانی از خروج مرحله‌ای موجودی طلا و فلزات برای آبشده، متفرقه، شرطی و سواله.
* محاسبه و نمایش تفکیکی «موجودی اولیه»، «خروج موقت در سند جاری» و «باقیمانده قابل انتخاب» در لیست انتخاب موجودی.
* اعمال محدودیت حداکثر وزن قابل انتخاب در Frontend و کنترل سخت‌گیرانه سمت Backend در هنگام ثبت سند.
* قفل‌سازی عیار موجودی انتخابی بر روی Frontend (Readonly) و اعتبارسنجی دقیق عیار در Backend با رکورد مرجع موجودی.
* آزادسازی کامل موجودی رزروشده هنگام لغو، ویرایش یا حذف ردیف‌های سند قبل از ثبت نهایی.
* پیشگیری از شرایط رقابتی (Race Condition) و خروج بیش از موجودی واقعی در دیتابیس.

### اعتبارسنجی و امنیت
* اعمال محدودیت عددی سخت‌گیرانه برای فیلد «شماره پاکت / انگ» در Frontend (حذف کاراکترهای غیرعددی) و اعتبارسنجی الگوی عددی `^[0-9]+$` در Backend.
* اعتبارسنجی مجدد عیار و اوزان در لایه Backend بدون اعتماد به داده‌های Client.

### رابط کاربری (UI/UX)
* اصلاح رنگ، پس‌زمینه، کادر، فوکوس و وضعیت غیرفعال فیلد «نام آزمایشگاه ری‌گیری» و هماهنگ‌سازی کامل آن با سایر ورودی‌های فرم در دو تم روشن و تاریک.
* نمایش Toast اطلاع‌رسانی با متن دقیق «سند با موفقیت نهایی شد.» پس از ثبت قطعی سند.

### دقت و کیفیت
* حفظ دقت اعشاری کامل در تمامی محاسبات وزنی و مالی با پشتیبانی از سیستم اعشار تنظیمات Zarfolio.
* اجرای تست‌های واحد برای پیش‌نمایش مانده، خروج مرحله‌ای موجودی و اعتبارسنجی شماره پاکت/انگ.
